import styled from "styled-components";

const LoaderLayout = styled.div`
    background-color: black;
    color: white;
`;

const Loader = () => {
    return <LoaderLayout>로더데쓰</LoaderLayout>;
};

export default Loader;
